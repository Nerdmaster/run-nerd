package run_nerd {
  import flash.display.MovieClip;
  import flash.events.Event;
  import flash.events.MouseEvent;

  public class IntroMovie extends MovieClip {
    public var on_finish;               // The function we need to call when any animating is done and user clicks "close"

    // Basic constructor to set everything up nicely
    public function IntroMovie() {
      // Set up event handlers - stage add/remove, button clicks, etc
    }
  }
}
